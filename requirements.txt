# Python dependencies for Altman Z-Score Analysis Tool
# Core dependencies - these are checked in the validation script
pandas>=2.2.0
numpy>=1.26.3
asyncio>=3.4.3  # Explicitly include asyncio which is checked in prerequisites
beautifulsoup4>=4.12.2  # Required for SEC EDGAR parsing
aiohttp>=3.9.3  # Required for SEC EDGAR API calls

# Other primary dependencies
pydantic>=2.6.1
requests>=2.31.0
yfinance>=0.2.35
matplotlib>=3.8.2
scipy>=1.12.0
python-dotenv>=1.0.1
openai>=1.12.0
pytest>=8.0.0
pytest-asyncio>=1.0.0
pytest-timeout>=2.3.1
tabulate>=0.9.0
finnhub-python>=2.4.19

# Data validation and processing
python-dateutil>=2.8.2
tqdm>=4.66.1  # For progress bars
pytz>=2024.1  # For timezone handling

# Output generation dependencies
plotly>=5.17.0  # For interactive charts
jinja2>=3.1.2  # For HTML template rendering
markdown>=3.4.4  # For Markdown support

# Additional SEC EDGAR integration dependencies
lxml>=4.9.3  # For XML/HTML processing (used by BeautifulSoup)
aiolimiter>=1.1.0  # For rate limiting SEC API requests
async-timeout>=4.0.3  # For handling timeouts in async operations
